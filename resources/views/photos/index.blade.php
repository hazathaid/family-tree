<x-layouts.app title="Arsip Foto"><div class="d-flex justify-content-between"><div><h1>Foto {{ $family->name }}</h1><p class="text-secondary">Kenangan keluarga lintas generasi.</p></div><a class="btn btn-primary align-self-start" href="{{ route('photos.create') }}">Unggah foto</a></div><div class="d-flex flex-wrap gap-2 my-4"><a class="btn btn-sm btn-outline-secondary" href="{{ route('photos.index') }}">Semua</a>@foreach($albums as $album)<a class="btn btn-sm btn-outline-secondary" href="{{ route('albums.show',$album) }}">{{ $album->name }} ({{ $album->photos_count }})</a>@endforeach</div>@if($photos->isEmpty())<x-empty-state title="Belum ada foto" message="Unggah foto pertama keluarga Anda." />@else<div class="row g-3">@foreach($photos as $photo)<div class="col-6 col-md-4 col-xl-3"><a href="{{ route('photos.show',$photo) }}"><img loading="lazy" class="img-fluid rounded w-100" style="aspect-ratio:1;object-fit:cover" src="{{ Storage::url($photo->thumbnail_path) }}" alt="{{ $photo->caption ?: 'Foto keluarga '.$family->name }}"></a></div>@endforeach</div><div class="mt-4">{{ $photos->links() }}</div>@endif</x-layouts.app>
