<x-layouts.app title="Detail Foto">@if(session('status'))<x-alert variant="success">{{ session('status') }}</x-alert>@endif<div class="row g-4"><div class="col-lg-8"><img class="img-fluid rounded" src="{{ Storage::url($photo->path) }}" alt="{{ $photo->caption ?: 'Foto keluarga '.$photo->family->name }}"></div><div class="col-lg-4"><h1 class="h3">{{ $photo->caption ?: 'Foto keluarga' }}</h1><p class="text-secondary">{{ $photo->album?->name ?: 'Tanpa album' }}</p><h2 class="h5">Anggota yang ditandai</h2><p>{{ $photo->taggedMembers->pluck('full_name')->join(', ') ?: 'Belum ada' }}</p>@can('update',$photo)<form method="POST" action="{{ route('photos.tags.update',$photo) }}">@csrf @method('PUT')<select class="form-select" name="member_uuids[]" multiple>@foreach($members as $member)<option value="{{ $member->uuid }}" @selected($photo->taggedMembers->contains('id',$member->id))>{{ $member->full_name }}</option>@endforeach</select><button class="btn btn-primary mt-2">Simpan tag</button></form>@endcan</div></div></x-layouts.app>
