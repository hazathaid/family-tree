<x-layouts.app title="Artikel Keluarga"><div class="d-flex justify-content-between align-items-center mb-4"><div><h1>Artikel {{ $family->name }}</h1><p class="text-secondary">Kisah dan kabar keluarga.</p></div><a class="btn btn-primary" href="{{ route('articles.create') }}">Tulis artikel</a></div>@if(session('status'))<x-alert variant="success">{{ session('status') }}</x-alert>@endif
<form class="row g-2 mb-4"><div class="col-md-5"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Cari artikel"></div><div class="col-md-4"><select class="form-select" name="category_uuid"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->uuid }}" @selected(request('category_uuid')===$category->uuid)>{{ $category->name }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-outline-primary">Filter</button></div></form>
@if($articles->isEmpty())<x-empty-state title="Belum ada artikel" message="Mulai dokumentasikan cerita keluarga." />@else<div class="row g-4">@foreach($articles as $article)<div class="col-md-6 col-xl-4"><x-card><div class="small text-secondary">{{ $article->category->name }} · {{ $article->status }}</div><h2 class="h5 mt-2"><a href="{{ route('articles.show',$article) }}">{{ $article->title }}</a></h2><p>{{ $article->excerpt ?: Str::limit(strip_tags($article->content),120) }}</p><span>♥ {{ $article->likes_count }} · 💬 {{ $article->comments_count }}</span></x-card></div>@endforeach</div><div class="mt-4">{{ $articles->links() }}</div>@endif</x-layouts.app>
