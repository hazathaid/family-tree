<x-layouts.app title="Audit Log">
    <div class="d-flex flex-wrap justify-content-between gap-3 mb-4"><div><a href="{{ route('admin.dashboard') }}">← Administrasi</a><h1 class="h2 mt-2">Audit log</h1></div><a class="btn btn-outline-primary align-self-end" href="{{ route('admin.audit-logs.export', request()->query()) }}">Ekspor CSV</a></div>
    <x-card class="mb-3"><form method="GET" class="row g-3"><div class="col-md-4"><label class="form-label" for="action">Tindakan</label><input class="form-control" id="action" name="action" value="{{ request('action') }}" maxlength="100"></div><div class="col-md-3"><label class="form-label" for="date_from">Dari tanggal</label><input class="form-control" type="date" id="date_from" name="date_from" value="{{ request('date_from') }}"></div><div class="col-md-3"><label class="form-label" for="date_to">Sampai tanggal</label><input class="form-control" type="date" id="date_to" name="date_to" value="{{ request('date_to') }}"></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100">Terapkan</button></div></form></x-card>
    <x-card :padding="false"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><caption class="visually-hidden">Catatan audit sistem</caption><thead><tr><th>Waktu</th><th>Pelaku</th><th>Tindakan</th><th>Target</th></tr></thead><tbody>@forelse($logs as $log)<tr><td class="text-nowrap">{{ $log->created_at?->translatedFormat('d M Y H:i') }}</td><td>{{ $log->user?->email ?? 'Sistem' }}</td><td><code>{{ $log->action }}</code></td><td>{{ class_basename($log->auditable_type) }}<br><small class="text-secondary">{{ $log->auditable_uuid }}</small></td></tr>@empty<tr><td colspan="4"><x-empty-state title="Audit log kosong" message="Belum ada tindakan yang cocok dengan filter." /></td></tr>@endforelse</tbody></table></div></x-card><div class="mt-3"><x-pagination :paginator="$logs" /></div>
</x-layouts.app>
