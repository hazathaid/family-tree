<x-layouts.app title="Acara Keluarga"><div class="d-flex justify-content-between"><div><h1>Acara {{ $family->name }}</h1><p class="text-secondary">Agenda keluarga mendatang dan terdahulu.</p></div><a class="btn btn-primary align-self-start" href="{{ route('events.create') }}">Buat acara</a></div><form class="row g-2 my-4"><div class="col-md-6"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Cari acara atau lokasi"></div><div class="col"><button class="btn btn-outline-primary" name="upcoming" value="1">Acara mendatang</button><a class="btn btn-link" href="{{ route('events.index') }}">Semua</a></div></form>@if($events->isEmpty())<x-empty-state title="Belum ada acara" message="Buat agenda keluarga berikutnya." />@else<div class="row g-3">@foreach($events as $event)<div class="col-md-6"><x-card><div class="small text-secondary">{{ $event->event_date->timezone(config('app.timezone'))->format('d M Y, H:i') }}</div><h2 class="h5"><a href="{{ route('events.show',$event) }}">{{ $event->title }}</a></h2><p>{{ $event->location ?: 'Lokasi belum ditentukan' }}</p><span>{{ $event->yes_count }} hadir · {{ $event->maybe_count }} mungkin</span></x-card></div>@endforeach</div><div class="mt-4">{{ $events->links() }}</div>@endif</x-layouts.app>
